step to follow after downloading project file from git 

1. npm install - this to for downloading node_modules

2.to connect to the database the url will be in .env file assigned to var MONGO_URI when we puh our data to git .env file will bw ignored so add .env file and assign link of the database from atlas to MONGO_URI

    syntax   ->>  MONGO_URI = url